---
title: "Parameter-Efficient Fine-Tuning"
year: "2023"
tags: [fine-tuning, peft, lora, adapters]
tldr: "Family of methods (LoRA, adapters, prefix tuning) that fine-tune only a small subset of parameters while keeping the backbone frozen, drastically reducing compute costs."
aliases: ["Parameter-Efficient-Fine-Tuning"]
---

## Stub

> This is a concept stub. See the notes that reference this page for context.

Family of methods (LoRA, adapters, prefix tuning) that fine-tune only a small subset of parameters while keeping the backbone frozen, drastically reducing compute costs.
