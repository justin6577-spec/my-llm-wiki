---
title: "Prefix Tuning"
year: "2021"
tags: [fine-tuning, peft, parameter-efficient]
tldr: "Prepends trainable continuous prefix tokens to each layer's attention context; only these prefix vectors are updated during fine-tuning while backbone weights stay frozen."
aliases: ["Prefix-Tuning"]
---

## Stub

> This is a concept stub. See the notes that reference this page for context.

Prepends trainable continuous prefix tokens to each layer's attention context; only these prefix vectors are updated during fine-tuning while backbone weights stay frozen.
