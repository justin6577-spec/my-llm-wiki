---
title: "Adapter Layers"
year: "2019"
tags: [fine-tuning, peft, parameter-efficient, transfer-learning]
tldr: "Small trainable bottleneck modules inserted between frozen pretrained layers — down-project, nonlinearity, up-project; only adapters are trained during fine-tuning."
aliases: ["Adapter-Layers"]
---

## Stub

> This is a concept stub. See the notes that reference this page for context.

Small trainable bottleneck modules inserted between frozen pretrained layers — down-project, nonlinearity, up-project; only adapters are trained during fine-tuning.
